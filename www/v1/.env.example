# Copie para .env e preencha com valores reais.
# NUNCA commite o .env (ja esta em .gitignore).

DB_ROOT_PASS=<troque-me>
DB_HOST=db-multicursos
DB_USER=multicursosdf
DB_PASS=<troque-me>
DB_NAME=dbmulticursos
